---
title: "Disable automatic terminal creation in VS Code"
author: "Gabriel Ortega-Solís"
execute:
  eval: false
---

# Goal

The goal is simple: your commands should always run in the terminal that is already open.

# Disable Automatic Terminal Creation in VS Code

VS Code may create extra terminals because of restored sessions, Python auto-activation, or debug/task behavior. Disable these triggers so you can keep working in one terminal.

## Open your settings JSON

Use **Ctrl+Shift+P** and run:

- `Preferences: Open User Settings (JSON)`

Then add (or merge) the following keys:

```json
{
  "terminal.integrated.hideOnStartup": "always",
  "terminal.integrated.enablePersistentSessions": false,
  "terminal.integrated.persistentSessionReviveProcess": "never",
  "python.terminal.activateEnvironment": false,
  "debug.internalConsoleOptions": "neverOpen"
}
```

With these settings, VS Code is less likely to open new terminals by itself. You can still open new terminals by clicking Terminal> New Terminal on the menu.